GEMINI_API_KEY=your_gemini_api_key_here

# Copy this file to `.env` (or set GEMINI_API_KEY in your environment) before running `npm run build`.
# NOTE: For production use, prefer a server-side proxy and do NOT embed the key in client bundles.
